import 'bootstrap/dist/css/bootstrap.min.css';
import React from 'react';
import ReactDOM from 'react-dom/client';
import NumberLister from './number_adder';

ReactDOM.createRoot(document.getElementById('root')).render(
    <NumberLister/>
)